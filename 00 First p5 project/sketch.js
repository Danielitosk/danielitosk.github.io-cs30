// Project Title
// Your Name
// Date
// One line description
// Extra for Experts:
// - describe what you did to take this project "above and beyond"



function setup() {
  //this happens exactyl once, at the start
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  //happens over and over(60fps)
  //background(220);
  circle(mouseX, mouseY,30);
}
